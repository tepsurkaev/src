const initialState = {
  items: [],
  loading: false,
  editing: false,
  deleting: false,
  adding: false,
  error: null,
};

const review = (state = initialState, action) => {
  switch (action.type){


    default:
      return state
  }
};
